# Daily Log — 27 August 2026

## Canonical Domain Cutover, Full-Site SEO/AI Metadata, Hero Whitespace Fix & Dependency Cleanup

### 1. Central SEO Config (`lib/seo.ts` — new)
- Single source of truth: `SITE_URL = 'https://sarvadnyainfotech.com'`, `SITE_NAME`, `SITE_TITLE`, `SITE_DESCRIPTION`, `SITE_CONTACT`.
- `seoMetadata({ title, description, path, keywords })` stamps canonical + OG + Twitter defaults on every page.
- JSON-LD builders: `orgJsonLd()`, `localBusinessJsonLd()`, `webSiteJsonLd()`, `faqJsonLd()`, `breadcrumbJsonLd()`.
- **Title de-duplication fix:** `seoMetadata` now returns the FINAL `<title>` as an `absolute` value and appends `| Sarvadnya Infotech LLP` only when the title doesn't already contain the brand. Fixes the home page rendering `Sarvadnya Infotech LLP - Tally Certified Partner Since 2008 | Sarvadnya Infotech LLP` (double brand). Home now clean; inner pages get exactly one suffix.

### 2. Canonical Domain Cutover (keep old `.vercel.app` working alongside, not replace)
- `app/sitemap.ts`: all URLs render from `SITE_URL`, added `/report-problem`.
- `app/robots.ts`: `Sitemap: ${SITE_URL}/sitemap.xml`.
- `next.config.js`: CSP `img-src`/`connect-src` now also allow `https://sarvadnyainfotech.com` + `https://www.sarvadnyainfotech.com`.
- `middleware.ts`: `DEFAULT_ALLOWED_ORIGINS` includes both new domains + old `.vercel.app`.
- `cpanel-landing/lib/api.ts` deliberately untouched (API-base change out of scope).
- Verified live: sitemap lists all 38 routes on `https://sarvadnyainfotech.com`; robots points at `https://sarvadnyainfotech.com/sitemap.xml`; canonical on every spot-checked page uses the new domain.

### 3. Per-Page Metadata (all ~40 routes)
- Root `app/layout.tsx`: `metadata` export (metadataBase = SITE_URL, title template, description, keywords, icons, OG/Twitter) + `orgJsonLd()` + `webSiteJsonLd()` injected at top of `<body>`.
- `app/(site)/layout.tsx`: metadata title template/default.
- `app/(site)/page.tsx` (home): metadata + `localBusinessJsonLd()` script (verified: 4 JSON-LD scripts render — Organization, LocalBusiness, WebSite + existing FAQ).
- For the 35 `'use client'` pages: metadata blocks moved into colocated server `layout.tsx` files (correct pattern — client components can't export `metadata`). Server pages keep direct `export const metadata`.
- Final coverage sweep confirmed all 40 `(site)` routes emit metadata: colocated layouts (client pages) OR direct `export const metadata` (server pages: `ask-sara`, `demo`, `news`, home). `/theme-settings` (a server component that had none) gained a `seoMetadata(...)` export to close the last gap.

### 4. Hero Whitespace Bug Fix (`app/components/HomeHero.tsx`)
- QuickAccess overhang `-mt-10 lg:-mt-12` → `-mt-16 lg:-mt-20` so the straddling cards sit flush against the hero video bottom, removing the leftover white band. Preserved the user's pre-existing `videoRef`/`playbackRate` removal.
- Verified via DOM measurement: card overhang −80px (flush against video bottom) and cards→next-section gap 0px.

### 5. Dependency Cleanup + Regression Caught by Build
- Removed unused packages from `package.json`: `class-variance-authority`, `ogl`, `radix-ui`, `react-colorful`, `xlsx` (deps) + `babel-plugin-react-compiler` (devDep). Ran `npm install` (removed 358 packages).
- **Build regression caught + fixed:** the pruned `shadcn` package was **actually required** via `@import "shadcn/tailwind.css"` in `app/globals.css` (CSS `@import`, missed by a TS-only grep). Restored `shadcn` to dependencies. `-8` package cleanup is now only the genuinely-unused ones.
- `npx tsc --noEmit` passes; `npm run build` passes (EXIT 0).

### 6. Incidental Finding — `cpanel-landing` files were deleted (working-tree)
- Noticed `cpanel-landing/` showed 16 uncommitted deletions (empty dir). Not caused by any intentional session edit. **Restored all 16 files from git HEAD** (`git checkout -- cpanel-landing`) so the subtree is intact; confirms changes were never committed and are fully reverted.

### Verification
- `npm run build` EXIT 0 (all routes + robots.txt + sitemap.xml compile).
- Live production server checks: sitemap.xml (38 URLs, new domain), robots.txt (new domain), canonical/title on home + `/about` `/contact` `/products` `/cloud` `/modules` `/addons` `/news` `/services` all correct (new domain canonical, single-suffix titles).
- Home JSON-LD: Organization + LocalBusiness + WebSite rendering.
- Hero geometry: overhang −80px, grid→section gap 0px.

---
