# Daily Log — 29 August 2026

## Zoho Widget Swap, Full-IP Collection (drop ipMasked), TallyCapital About Us & Footer Logo Fix

### 1. Zoho SalesIQ — new widget ID (tracking-only preserved) (`app/layout.tsx`)
- Swapped the `#zsiqscript` src to the new SalesIQ widget (`wc=siq2206f6c142ca693dad0cc4071612c0952f6f7bc1cf08e71cd693f685913813b346a77e21e757c80042de5e8938dd0719`).
- The inline `$zoho` init + chat-hide ready-hook were kept **verbatim** (previously working pattern): init script runs before the deferred zsiqscript, CSS suppression layer in `globals.css` stays → embed remains purely tracking-only on the same allowed host/domain, so no CSP change and no new failure mode.

### 2. Full IP everywhere — `ipMasked` dropped (per user: collect all, store openly)
- Full IP was already persisted in the `ip` field on every record; the redundant masked variant is now gone end-to-end:
  - `lib/visitors.ts` — removed `ipMasked` from `VisitorRecord` + `recordVisitor` `$set`. `maskIp()` kept for console logs only.
  - `app/api/contact/route.ts` + `app/api/email/submit/route.ts` — removed `maskIp` import + `ipMasked` from `submissionData`.
  - `app/api/tss-renewal/route.ts` + `app/api/problem-reports/route.ts` — removed `maskIp` import + `ipMasked` from `enriched` and the `sendEmailDirect` submission payload.
  - `app/actions/careers.ts` — removed `maskIp` import + `ipMasked` from `saveApplication`.
  - `lib/email.ts` — removed `ipMasked` from `FormSubmissionPayload`; internal email copy now shows the **full** IP row.
  - `lib/mongodb-utils.ts` — removed `ipMasked` from the search `$or` fields in both `getSubmissionsPaginated` and `exportAllSubmissions` (`ip` remains searchable).

### 3. TallyCapital About Us (`app/(site)/products/tallycapital/page.tsx`)
- **Address:** About Us address now renders the office address from the **site settings DB** fetched via `/api/settings` (`office_address`), replacing the hardcoded `123, Business Center, Main Road, Pune - 411001` placeholder. Neutral fallback `Vindya Complex, Belapur, Navi Mumbai` while loading/unset. (Page is a client component, so a `useEffect` fetch mirrors the Footer pattern.)
- **12+ years of association:** paragraph "authorised Tally Partners for over 20 years" → "associated as authorised Tally Partners for 12+ years"; right-side "Why Choose" bullet "Over 20 years as Certified Tally Partner" → "12+ years of association as Certified Tally Partner" (both references per user confirmation).

### 4. Footer logo alignment fix (`app/components/Footer.tsx`)
- Logo block restacked **vertically** (image → brand name → tagline) inside the narrow `lg:col-span-1` desktop column instead of the old horizontal flex (image + text side-by-side) that overflowed/overlapped on the right.
- Image container `h-11 w-auto max-w-full shrink-0`, text spans `min-w-0` so the tracking-widest tagline wraps/contains instead of pushing past the column edge. Same `/TallyCertificate.png` brand asset retained.

### Verification
- `grep ipMasked` → only documentation comments remain (no code refs).
- `npx tsc --noEmit` and `npm run build` pass (see session).

---
*Docs: this log + `// CHANGE: 2026-08-29` inline comments on every touched line.*