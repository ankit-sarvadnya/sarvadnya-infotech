# Daily Log — 11 September 2026

## UI Polish Session: Hero Image Fit, Footer Typography, WhatsApp Showcase, Cloud Pages (6 commits)

### 1. Hero Image Fit — Mobile App Biz + TSS (commit `ac5084f`)
- **Framed to the brand pattern:** compact single static background image, right-anchored with **natural aspect ratio** (`h-full w-auto`, `width={2752} height={1536}`), hidden below `md` — no `object-cover` cropping.
- Began with a requested 2-image auto slider (biz → ba) then **reverted to a single image** per client ("i don't want multi changing bg, only one biz bg.png, and same with tss page").
- **Overflow/crop fix:** after client reported top/bottom cropping, removed `object-cover` + fixed heights in favour of the right-anchored natural-ratio image that clips only at the left edge.
- **Left-fade overlay added then removed** — client: "some white gradient from left to transparent right is seen at left end of images, remove it" → both overlays deleted.
- **Colors:** biz hero `bg-[#F3EEE5]`, `md:min-h-[400px]`; TSS hero `bg-[#F1EDE5]`, `md:min-h-[320px]` (was cream gradient / `md:aspect-[16/9]`).
- Assets: `public/biz bg.png` (NEW, 3.2 MB), `public/tss-icon.png` + `public/footer png inverted.png` updated.

### 2. Tally-on-WhatsApp Page (commits `ac5084f`, `cb32545`, `6dd457d`)
- Hero plate frame corrected to `md:aspect-[11/6]` matching the real source `public/tallytowa.png` (2816×1536) + proper `sizes` attribute.
- **Bottom showcase banner** added (right-anchored `/tallytowa.png`), then re-fit into an `aspect-[11/6]` rounded frame (no overflow), then **removed entirely** per client and the image repositioned into the **"Professional, Fast, and 100% Automated"** ("ZERO FRICTION DETAILS") **left column** (`order-2 lg:order-1`, `aspect-[11/6]` rounded frame). Unused `Activity` icon import dropped.

### 3. FAQ + Sara Chatbot Copy (commit `ac5084f`)
- `app/components/faq.tsx` + `lib/sara-topics.ts` — FAQ/answer CTA text corrected from "contact us" → **"contact Sarvadnya Infotech LLP"**.

### 4. ERP Matcher Fix — Sara Keyword Tests (commit `ac5084f`)
- `scripts/sara-topics-test.mjs` was failing on the ERP-renewal phrase.
- Root cause: `matchTopic()` word-boundary scoring only matched the label/synonyms; `"erp9 renewal"` scored below threshold.
- Fix: added phrase keywords `erp9 renewal`, `erp 9 renewal`, `erp renewals`, `erp 9 renewals` to the ERP topic in `lib/sara-topics.ts`.
- **23/23 PASS** on the standalone suite.

### 5. Footer Typography (commit `ac5084f`)
- Titles (h3/h5) stay `font-black`; **all body content demoted**: intro paragraph, 4 link columns, email, and phone lines now `font-normal` + `text-white/70` (teal hover retained) per client ("footer only keep name of title in bold, rest below content lower weight and lighter").

### 6. HomeHero Video Posters (commit `6f9c9b0`)
- Both mobile + desktop `<video>` `poster` attributes changed from `/logo.png` to **`/video-thumbnail.png`** (the new 1280×720 asset from 2026-09-08).

### 7. Homepage VideoObject Removed (commit `d195375`)
- `lib/seo.ts` VideoObject builder + `app/(site)/page.tsx` usage removed — Google's GSC validation flags background hero videos as "Not a watch page", so the schema was invalid and is gone.

### 8. Cloud Pages — Background + Pricing (commits `6f9c9b0`, `389d837`)
- All 4 cloud pages — `/cloud/tallycloudaccess`, `/cloud/aws`, `/cloud/windows`, `/cloud/backup-for-tally` — background `#f5f7fa` (bluish gray) → `#F6F6F6` → **`#FBFAF7`** (client's final colour choice).
- **Tally Cloud Access pricing section reworked:** removed the 3 plan cards (Basic / Standard / Professional — all "NA" price) and the `plans` array; the "Simple Cloud Pricing" grid is now a single section with **"Contact Sales Team"** (opens the quote modal) + **"Contact Us"** buttons. Sticky-nav label `Plans & Pricing` → `Pricing`; header CTA `View Plans` → `See Pricing`.

### Verification
- `npm run typecheck` clean; `node scripts/sara-topics-test.mjs` → 23/23 PASS.
- Pushed: `ac5084f`, `cb32545`, `6dd457d`, `6f9c9b0`, `389d837` (origin redirects Orthodox2000 → ankit-sarvadnya).

---
*Docs: this log, `daily logs/excel logs.csv`, `// CHANGE: 2026-09-11` inline comments in the edited pages.*