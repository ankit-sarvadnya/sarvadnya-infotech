# Daily Log — 16 September 2026

## Certified Industry Partners — Radial Network Graph Redesign (Homepage)

All changes to `app/components/CertifiedPartners.tsx` + `app/globals.css` (`// CHANGE: 2026-09-16` inline comments).

### 1. Flat logo-card row -> Radial network graph
- Replaced the flex-wrap card grid with a hub-and-spoke graph after the provided `DependencyGraph` reference presentation.
- **TallyPrime is the fixed center hub** (dark node -> TallyPrime logo badge on white).
- Each brand card radiates outward on its own **animated bezier connector** (`.connect-flow` dash animation added to `globals.css`; CSS-only, honors `prefers-reduced-motion`, default 2.2s loop, 0.9s on hover).
- Percentage viewBox `0 0 100 100` matches CSS `top`/`left` exactly, so positions scale perfectly with the aspect-ratio stage (no JS measurement needed).
- Subtle responsive background grid (5%), rounded white stage, shared hover dimming of non-hovered connectors.
- **Parent wrapper divs, section gradient, eyebrow line ("Our Global Network") and H2 ("Certified Industry Partners") are preserved byte-for-byte.**

### 2. Brand-exact connector colors (owner spec)
- `BRAND_STROKES` map: **Biz Analyst `#38bdf8` (light blue), OTU `#f59e0b` (yellow), AWS `#f97316` (orange), NoSky `#0f172a` (black)**.
- Exact brand-name match wins; unknown partner names fall back to NoSky black.
- Hover ring colors follow the same brand key.

### 3. TallyPrime logo center badge
- Replaced the "TP" monogram with the actual `/tallyprime logo.png` on a white rounded badge with soft teal ring + brand-teal glow.
- TallyPrime is filtered out of the peripheral ring so it renders exactly once (fix: it was showing both as center AND a peripheral node).

### 4. Responsive layout split (via `useLg()` matchMedia at 1024px)
- **mobile / md (default):** square 1:1 stage (`aspect-square`), cardinal N/E/S/W positions (`polarPosMobile`, radius 40%).
- **lg+ (desktop):** `max-w-6xl` stage, wide `2:1` aspect (`lg:aspect-[2/1]`), cards 120° apart, side-focused (`polarPosDesktop` — 0°/120°/240°/270°, radius 30%).
- **Containers enlarged** per client feedback ("too small"): peripheral cards `w-24` (96px) -> `lg:w-32` (128px); center badge `w-16` (64px) -> `lg:w-24` (96px).
- Node auto-distribution supports future partners in `lib/partners.ts` without coordinate edits.

### 5. Legacy data flow untouched
- Still server-fetched via `getPartners('brand')` in `app/(site)/page.tsx` (and `/demo`), reconciled to the canonical 5-item list in `lib/partners.ts`; IntersectionObserver visibility gating, loading skeleton, and `fetchWithCache` fallback preserved.

### Verification
- `npm run typecheck` — clean.
- Full production build (`npm run clean` + `npm run build`) — 87 pages, EXIT 0.
- Live DOM checks via Puppeteer at 1280 / 768 / 390 px: correct per-breakpoint positions, exact brand stroke colors, all 5 logos loaded (`rendered: true`).
- Build discipline followed: dev server stopped before build, `.next` cleaned.

### Docs
- AGENTS.md `CertifiedPartners` row updated (radial graph, brand colors, `useLg()` split, container sizes).
- This log + `daily logs/excel logs.csv` row appended.

---

## Assigned Work — Tomorrow (2026-09-17): X-Format Layout Fix

**STATUS: ✅ COMPLETED on 2026-09-17 — see `daily logs/2026-09-17.md` and the `17-09-26` row in `excel logs.csv`.**

**Pending:** Client requested the desktop layout become an **X format** — **2 cards on the left, 2 cards on the right, only 60° between them on each side**, which consumes minimum height.

**How to implement (when in scope):**
1. `app/components/CertifiedPartners.tsx` — replace `polarPosDesktop` angles `[0, 120, 240, 270]` with the X layout:
   - left two cards ±30° around horizontal-left (i.e. **150° and 210°**)
   - right two cards ±30° around horizontal-right (i.e. **30° and 330°**)
   - i.e. `DESKTOP_ANGLES_DEG = [30, 330, 150, 210]` (or matching side grouping), radius ~30.
2. This yields a compact X: vertical spread only ~30% of stage height (cards near the horizontal center line) -> minimum section height.
3. Keep mobile/md cardinal layout unchanged.
4. Re-verify via Puppeteer at 1280 px (positions + logo load + stroke colors), then `npm run typecheck` + clean build.
5. Update AGENTS.md `polarPosDesktop` description + this log.

---
*Docs: this log, `daily logs/excel logs.csv`, `// CHANGE: 2026-09-16` inline comments in the edited files.*