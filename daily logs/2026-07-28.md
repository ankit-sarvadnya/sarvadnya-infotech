# Daily Log — 28 July 2026

## Sara AI Chatbot — Dual-Mode Split, TSS Renewal System & UI Polish

### Files Modified
| # | File | Change |
|---|------|--------|
| 1 | `lib/sara-topics.ts` | Added `getTeachingFallbackResponse()` — 16 teaching-focused regex patterns (GST, inventory, banking, payroll, reports, shortcuts, vouchers, e-invoicing, e-way bills, backup, cloud, errors, pricing redirect) with step-by-step instructions instead of sales pitches. Price questions redirect to sales team. Generic fallbacks ask what you want to learn. |
| 2 | `app/(site)/learn-sara/page.tsx` | Complete rewrite — Welcome message changed to teaching-focused ("I'm Sara, your TallyPrime learning assistant"). Input placeholder updated. Replaced local `getFallbackResponse()` with `getTeachingFallbackResponse()`. Added Groq AI API integration for non-topic-matched queries (`mode: "learn"`). Falls back to local teaching response on AI error. Full conversation history sent to AI for context-aware replies. |
| 3 | `app/api/chat/route.ts` | Added `mode` parameter to POST body — `"learn"` vs `"ask"` (default). Split system prompt into `SALES_SYSTEM_PROMPT` (original sales consultant) and `LEARN_SYSTEM_PROMPT` (patient, fun teacher). Learn mode handles ANY question — Tally, non-Tally, random, jokes — and gently steers back. Injection attempts get mode-appropriate responses (playful for learn, redirect for ask). Removed all console.log/debug statements. |
| 4 | `app/components/TssRenewalForm.tsx` | **NEW** — Reusable TSS renewal form component with 3 variants: `default` (full section with benefits), `compact` (inline in pricing tables), `inline` (embedded in page sections). Form fields: serial number, name, email (all mandatory). "Renew Now" button saves to MongoDB `tss_renewals` collection. Success/error states with animated feedback. |
| 5 | `app/api/tss-renewal/route.ts` | **NEW** — POST endpoint that validates required fields (serialNumber, name, email) and saves renewal request to MongoDB `tss_renewals` collection with timestamps. |
| 6 | `app/api/admin/tss-renewals/route.ts` | **NEW** — GET/PATCH/DELETE endpoints for admin management of TSS renewal requests. GET returns all renewals sorted by newest first. PATCH updates status (pending/approved/rejected). DELETE removes a renewal record. |
| 7 | `app/admin/tss-renewals/page.tsx` | **NEW** — Admin page for managing TSS renewal requests with status badges, approve/reject buttons, delete functionality, and empty state handling. |
| 8 | `lib/mongodb-utils.ts` | Added 4 new functions: `saveTssRenewal()`, `getTssRenewals()`, `updateTssRenewalStatus()`, `deleteTssRenewal()` for TSS renewal CRUD operations. |
| 9 | `app/api/admin/stats/route.ts` | Added `tssRenewals` count to dashboard stats response. |
| 10 | `app/admin/AdminSidebar.tsx` | Added "TSS Renewals" nav link with clipboard icon between Submissions and Problem Reports. |
| 11 | `app/admin/page.tsx` | Added TSS Renewals stat card (clipboard icon, emerald accent) in the stats grid. |
| 12 | `app/(site)/services/tss/page.tsx` | Added `TssRenewalForm` import and embedded inline variant replacing the old CTA section. |
| 13 | `app/(site)/products/tallydrive/page.tsx` | Added compact `TssRenewalForm` in pricing table footer section. |
| 14 | `app/(site)/services/page.tsx` | Added compact `TssRenewalForm` in TSS Renewal service detail popup. |
| 15 | `app/components/faq.tsx` | Added pinned "IMPORTANT" ERP renewal notice at top of FAQ — amber warning badge, "Has Tally stopped ERP renewals?" question with answer explaining upgrade is the only option. |

### Summary of Changes

1. **Learn Sara vs Ask Sara Split** — Two distinct experiences:
   - **Ask Sara** (floating button): AI-powered sales consultant with product catalog, CTAs, and `getFallbackResponse()` for local fallback.
   - **Learn Sara** (`/learn-sara`): Teaching assistant that answers ANY question via Groq AI (`mode: "learn"`), with `getTeachingFallbackResponse()` as local fallback. Same `matchTopic()` for 7 core Tally topics.

2. **AI API Dual-Mode** — Single `/api/chat` endpoint now accepts `mode` parameter. Learn mode uses `LEARN_SYSTEM_PROMPT` (patient teacher, handles non-Tally questions playfully). Ask mode uses `SALES_SYSTEM_PROMPT` (proactive consultant, always sells).

3. **TSS Renewal System** — Complete CRUD system: user-facing form (3 variants) → API endpoint → MongoDB storage → admin dashboard with status management. Forms placed on TSS page, TallyDrive pricing, and Services page.

4. **Homepage FAQ ERP Notice** — Pinned amber "IMPORTANT" notice in FAQ explaining ERP renewal has stopped and upgrading to TallyPrime is the only option.

5. **Full Route Map in AI Prompts** — Both Sara prompts now contain complete site navigation (40+ public routes). Cross-references: Ask Sara redirects to Learn Sara for teaching; Learn Sara redirects to Ask Sara for pricing/purchase.

6. **Debug Cleanup** — Removed all `console.log`/`warn`/`error` from `learn-sara/page.tsx` and `app/api/chat/route.ts`.

### Session Continuation from 27 July
This session continued work from 27 July where the Sara chatbot was overhauled with AI API integration, performance fixes, and shared knowledge base. Today's work splits the experience into two distinct modes and adds the TSS renewal business feature.
