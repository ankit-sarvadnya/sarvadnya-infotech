# Daily Log — 31 August 2026

## Navbar Company-Name Cleanup & News Refresh

### 1. Navbar logo text (`app/components/Navbar.tsx`)
- Kept only the company name "Sarvadnya Infotech LLP"; removed the "Tally Certified Partner" subtitle line (per user, keep company name only).
- Reworked into a **two-line wordmark spread to match the logo's height** (per user): "Sarvadnya Infotech" on line 1 + "LLP" on line 2, wrapped in a `flex flex-col justify-center` so the two lines fill the tall `h-full` logo and stay vertically centred to it.
- **Colour scheme (industry-standard professional treatment):** "Sarvadnya Infotech" in dark graphite `slate-900` (primary name) + "LLP" in the brand teal `#006569` as the accent — the standard corporate wordmark pattern, instead of the earlier flat single-teal name.

### 2. News cleanup (MongoDB `news` collection + `lib/mongodb-utils.ts`)
- Removed the outdated "Sarvadnya Infotech LLP Awarded Top Certified Partner 2026" news item (`_id 6a0b08276c63b24c19a4c79a`) — this was the first item shown in the navbar news marquee.
- `fetchNews()` now sorts the feed so the **latest** item (parseable `date`, newest first) appears at the top; entries without a parseable date fall back to insertion order (`_id`). Applies to both the `/news` page (server) and the navbar/news marquee (`/api/news`).

### Verification
- MongoDB query confirmed the award item deleted; 3 news items remain (ERP 9 → June 20, TallyPrime v7.0 → May 12, Webinar → May 05 = newest-first).
- `npx tsc --noEmit` / build pending (see session).

---
*Docs: this log + `// CHANGE: 2026-08-31` inline comments on every touched line.*

### 3. Zoho SalesIQ — exact snippet rendered, hidden via opacity (`app/layout.tsx` + `app/globals.css`)
- `app/layout.tsx` now renders the Zoho 2-tag snippet **verbatim** (inline `window.$zoho` init + `#zsiqscript` widget script) exactly as provided, adjacent in one block at the end of `<body>` — nothing split in `<head>`.
- Removed the separate hide-hook inline script (`floatbutton.visible`/`chatbutton.visible`) — the user wants only the canonical snippet rendered.
- `app/globals.css` now hides the chatbox/consent banners via **`opacity: 0 !important`** (NOT `display:none`), so the widget stays mounted and trackable while invisible. Expanded selector list to cover consent bars and welcome/prechat mounts.
